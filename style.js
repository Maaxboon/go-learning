// Single line comments
if (isNegativeNumber(value)) {
  return false; // We don't know how to handle negative numbers
}

/*Multi
line
comments */

// Functions, methods or variable names should be camelcase
// Assigning a variable
let fourAndTwentyBLackBirds = 24;

// Defining a function
function thisIsAGreatMethod() {
  // Do some stuff
}

// filenames typically use snake_case or kebab-case: all lowercase letters with individual words separated with underscores(_) or hyphens(-)

hello_world.js; // snake_case
hello - world.js; // kebab-case
